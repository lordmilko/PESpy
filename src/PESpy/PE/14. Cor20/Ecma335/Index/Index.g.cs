/*********************************************************************
 * This code was generated by a tool.                                *
 * Please do not modify this file directly - modify Index.tt instead *
 ********************************************************************/

using System;

namespace PESpy.Ecma335
{
    public readonly struct Index
    {
        public readonly int RowId;

        public bool IsNil => RowId == 0;

        //CorTokenType only captures publically consumed indices. It doesn't capture internal ones

        //The type of a handle is not stored on disk; it's either implied by the spec, or is stored as a coded index
        //which we've just decoded and turned into an index with a known type
        internal Index(int rowId)
        {
            RowId = rowId;
        }

        public static explicit operator Index(int value) => new Index(value);

        public static explicit operator int(Index value) => value.RowId;

        public override string ToString() => RowId.ToString();
    }

    public readonly partial struct AssemblyOSIndex : IEquatable<AssemblyOSIndex>
    {
        public readonly int RowId;

        public bool IsNil => RowId == 0;

        public AssemblyOSIndex(int rowId)
        {
            RowId = rowId;
        }

        public static bool operator ==(AssemblyOSIndex left, AssemblyOSIndex right) => left.RowId == right.RowId;

        public static bool operator !=(AssemblyOSIndex left, AssemblyOSIndex right) => left.RowId != right.RowId;

        public static explicit operator AssemblyOSIndex(int value) => new AssemblyOSIndex(value);

        //Can't use implicit operator here, as for some reason this has a backwards effect of allowing other indices to be passed to our tables, due to the presence of a general purpose int indexer
        public static explicit operator int(AssemblyOSIndex value) => value.RowId;

        public override bool Equals(object other)
        {
            if (other is not AssemblyOSIndex v)
                return false;

            return Equals(v);
        }

        public bool Equals(AssemblyOSIndex other) => RowId == other.RowId;

        public override int GetHashCode() => RowId.GetHashCode();

        public override string ToString() => RowId.ToString();
    }

    public readonly partial struct AssemblyProcessorIndex : IEquatable<AssemblyProcessorIndex>
    {
        public readonly int RowId;

        public bool IsNil => RowId == 0;

        public AssemblyProcessorIndex(int rowId)
        {
            RowId = rowId;
        }

        public static bool operator ==(AssemblyProcessorIndex left, AssemblyProcessorIndex right) => left.RowId == right.RowId;

        public static bool operator !=(AssemblyProcessorIndex left, AssemblyProcessorIndex right) => left.RowId != right.RowId;

        public static explicit operator AssemblyProcessorIndex(int value) => new AssemblyProcessorIndex(value);

        //Can't use implicit operator here, as for some reason this has a backwards effect of allowing other indices to be passed to our tables, due to the presence of a general purpose int indexer
        public static explicit operator int(AssemblyProcessorIndex value) => value.RowId;

        public override bool Equals(object other)
        {
            if (other is not AssemblyProcessorIndex v)
                return false;

            return Equals(v);
        }

        public bool Equals(AssemblyProcessorIndex other) => RowId == other.RowId;

        public override int GetHashCode() => RowId.GetHashCode();

        public override string ToString() => RowId.ToString();
    }

    public readonly partial struct AssemblyRefOSIndex : IEquatable<AssemblyRefOSIndex>
    {
        public readonly int RowId;

        public bool IsNil => RowId == 0;

        public AssemblyRefOSIndex(int rowId)
        {
            RowId = rowId;
        }

        public static bool operator ==(AssemblyRefOSIndex left, AssemblyRefOSIndex right) => left.RowId == right.RowId;

        public static bool operator !=(AssemblyRefOSIndex left, AssemblyRefOSIndex right) => left.RowId != right.RowId;

        public static explicit operator AssemblyRefOSIndex(int value) => new AssemblyRefOSIndex(value);

        //Can't use implicit operator here, as for some reason this has a backwards effect of allowing other indices to be passed to our tables, due to the presence of a general purpose int indexer
        public static explicit operator int(AssemblyRefOSIndex value) => value.RowId;

        public override bool Equals(object other)
        {
            if (other is not AssemblyRefOSIndex v)
                return false;

            return Equals(v);
        }

        public bool Equals(AssemblyRefOSIndex other) => RowId == other.RowId;

        public override int GetHashCode() => RowId.GetHashCode();

        public override string ToString() => RowId.ToString();
    }

    public readonly partial struct AssemblyRefProcessorIndex : IEquatable<AssemblyRefProcessorIndex>
    {
        public readonly int RowId;

        public bool IsNil => RowId == 0;

        public AssemblyRefProcessorIndex(int rowId)
        {
            RowId = rowId;
        }

        public static bool operator ==(AssemblyRefProcessorIndex left, AssemblyRefProcessorIndex right) => left.RowId == right.RowId;

        public static bool operator !=(AssemblyRefProcessorIndex left, AssemblyRefProcessorIndex right) => left.RowId != right.RowId;

        public static explicit operator AssemblyRefProcessorIndex(int value) => new AssemblyRefProcessorIndex(value);

        //Can't use implicit operator here, as for some reason this has a backwards effect of allowing other indices to be passed to our tables, due to the presence of a general purpose int indexer
        public static explicit operator int(AssemblyRefProcessorIndex value) => value.RowId;

        public override bool Equals(object other)
        {
            if (other is not AssemblyRefProcessorIndex v)
                return false;

            return Equals(v);
        }

        public bool Equals(AssemblyRefProcessorIndex other) => RowId == other.RowId;

        public override int GetHashCode() => RowId.GetHashCode();

        public override string ToString() => RowId.ToString();
    }

    public readonly partial struct AssemblyRefIndex : IEquatable<AssemblyRefIndex>
    {
        public readonly int RowId;

        public bool IsNil => RowId == 0;

        public AssemblyRefIndex(int rowId)
        {
            RowId = rowId;
        }

        public static bool operator ==(AssemblyRefIndex left, AssemblyRefIndex right) => left.RowId == right.RowId;

        public static bool operator !=(AssemblyRefIndex left, AssemblyRefIndex right) => left.RowId != right.RowId;

        public static explicit operator AssemblyRefIndex(int value) => new AssemblyRefIndex(value);

        //Can't use implicit operator here, as for some reason this has a backwards effect of allowing other indices to be passed to our tables, due to the presence of a general purpose int indexer
        public static explicit operator int(AssemblyRefIndex value) => value.RowId;

        public override bool Equals(object other)
        {
            if (other is not AssemblyRefIndex v)
                return false;

            return Equals(v);
        }

        public bool Equals(AssemblyRefIndex other) => RowId == other.RowId;

        public override int GetHashCode() => RowId.GetHashCode();

        public override string ToString() => RowId.ToString();
    }

    public readonly partial struct AssemblyIndex : IEquatable<AssemblyIndex>
    {
        public readonly int RowId;

        public bool IsNil => RowId == 0;

        public AssemblyIndex(int rowId)
        {
            RowId = rowId;
        }

        public static bool operator ==(AssemblyIndex left, AssemblyIndex right) => left.RowId == right.RowId;

        public static bool operator !=(AssemblyIndex left, AssemblyIndex right) => left.RowId != right.RowId;

        public static explicit operator AssemblyIndex(int value) => new AssemblyIndex(value);

        //Can't use implicit operator here, as for some reason this has a backwards effect of allowing other indices to be passed to our tables, due to the presence of a general purpose int indexer
        public static explicit operator int(AssemblyIndex value) => value.RowId;

        public override bool Equals(object other)
        {
            if (other is not AssemblyIndex v)
                return false;

            return Equals(v);
        }

        public bool Equals(AssemblyIndex other) => RowId == other.RowId;

        public override int GetHashCode() => RowId.GetHashCode();

        public override string ToString() => RowId.ToString();
    }

    public readonly partial struct ClassLayoutIndex : IEquatable<ClassLayoutIndex>
    {
        public readonly int RowId;

        public bool IsNil => RowId == 0;

        public ClassLayoutIndex(int rowId)
        {
            RowId = rowId;
        }

        public static bool operator ==(ClassLayoutIndex left, ClassLayoutIndex right) => left.RowId == right.RowId;

        public static bool operator !=(ClassLayoutIndex left, ClassLayoutIndex right) => left.RowId != right.RowId;

        public static explicit operator ClassLayoutIndex(int value) => new ClassLayoutIndex(value);

        //Can't use implicit operator here, as for some reason this has a backwards effect of allowing other indices to be passed to our tables, due to the presence of a general purpose int indexer
        public static explicit operator int(ClassLayoutIndex value) => value.RowId;

        public override bool Equals(object other)
        {
            if (other is not ClassLayoutIndex v)
                return false;

            return Equals(v);
        }

        public bool Equals(ClassLayoutIndex other) => RowId == other.RowId;

        public override int GetHashCode() => RowId.GetHashCode();

        public override string ToString() => RowId.ToString();
    }

    public readonly partial struct ConstantIndex : IEquatable<ConstantIndex>
    {
        public readonly int RowId;

        public bool IsNil => RowId == 0;

        public ConstantIndex(int rowId)
        {
            RowId = rowId;
        }

        public static bool operator ==(ConstantIndex left, ConstantIndex right) => left.RowId == right.RowId;

        public static bool operator !=(ConstantIndex left, ConstantIndex right) => left.RowId != right.RowId;

        public static explicit operator ConstantIndex(int value) => new ConstantIndex(value);

        //Can't use implicit operator here, as for some reason this has a backwards effect of allowing other indices to be passed to our tables, due to the presence of a general purpose int indexer
        public static explicit operator int(ConstantIndex value) => value.RowId;

        public override bool Equals(object other)
        {
            if (other is not ConstantIndex v)
                return false;

            return Equals(v);
        }

        public bool Equals(ConstantIndex other) => RowId == other.RowId;

        public override int GetHashCode() => RowId.GetHashCode();

        public override string ToString() => RowId.ToString();
    }

    public readonly partial struct CustomAttributeIndex : IEquatable<CustomAttributeIndex>
    {
        public readonly int RowId;

        public bool IsNil => RowId == 0;

        public CustomAttributeIndex(int rowId)
        {
            RowId = rowId;
        }

        public static bool operator ==(CustomAttributeIndex left, CustomAttributeIndex right) => left.RowId == right.RowId;

        public static bool operator !=(CustomAttributeIndex left, CustomAttributeIndex right) => left.RowId != right.RowId;

        public static explicit operator CustomAttributeIndex(int value) => new CustomAttributeIndex(value);

        //Can't use implicit operator here, as for some reason this has a backwards effect of allowing other indices to be passed to our tables, due to the presence of a general purpose int indexer
        public static explicit operator int(CustomAttributeIndex value) => value.RowId;

        public override bool Equals(object other)
        {
            if (other is not CustomAttributeIndex v)
                return false;

            return Equals(v);
        }

        public bool Equals(CustomAttributeIndex other) => RowId == other.RowId;

        public override int GetHashCode() => RowId.GetHashCode();

        public override string ToString() => RowId.ToString();
    }

    public readonly partial struct CustomDebugInformationIndex : IEquatable<CustomDebugInformationIndex>
    {
        public readonly int RowId;

        public bool IsNil => RowId == 0;

        public CustomDebugInformationIndex(int rowId)
        {
            RowId = rowId;
        }

        public static bool operator ==(CustomDebugInformationIndex left, CustomDebugInformationIndex right) => left.RowId == right.RowId;

        public static bool operator !=(CustomDebugInformationIndex left, CustomDebugInformationIndex right) => left.RowId != right.RowId;

        public static explicit operator CustomDebugInformationIndex(int value) => new CustomDebugInformationIndex(value);

        //Can't use implicit operator here, as for some reason this has a backwards effect of allowing other indices to be passed to our tables, due to the presence of a general purpose int indexer
        public static explicit operator int(CustomDebugInformationIndex value) => value.RowId;

        public override bool Equals(object other)
        {
            if (other is not CustomDebugInformationIndex v)
                return false;

            return Equals(v);
        }

        public bool Equals(CustomDebugInformationIndex other) => RowId == other.RowId;

        public override int GetHashCode() => RowId.GetHashCode();

        public override string ToString() => RowId.ToString();
    }

    public readonly partial struct DeclSecurityIndex : IEquatable<DeclSecurityIndex>
    {
        public readonly int RowId;

        public bool IsNil => RowId == 0;

        public DeclSecurityIndex(int rowId)
        {
            RowId = rowId;
        }

        public static bool operator ==(DeclSecurityIndex left, DeclSecurityIndex right) => left.RowId == right.RowId;

        public static bool operator !=(DeclSecurityIndex left, DeclSecurityIndex right) => left.RowId != right.RowId;

        public static explicit operator DeclSecurityIndex(int value) => new DeclSecurityIndex(value);

        //Can't use implicit operator here, as for some reason this has a backwards effect of allowing other indices to be passed to our tables, due to the presence of a general purpose int indexer
        public static explicit operator int(DeclSecurityIndex value) => value.RowId;

        public override bool Equals(object other)
        {
            if (other is not DeclSecurityIndex v)
                return false;

            return Equals(v);
        }

        public bool Equals(DeclSecurityIndex other) => RowId == other.RowId;

        public override int GetHashCode() => RowId.GetHashCode();

        public override string ToString() => RowId.ToString();
    }

    public readonly partial struct DocumentIndex : IEquatable<DocumentIndex>
    {
        public readonly int RowId;

        public bool IsNil => RowId == 0;

        public DocumentIndex(int rowId)
        {
            RowId = rowId;
        }

        public static bool operator ==(DocumentIndex left, DocumentIndex right) => left.RowId == right.RowId;

        public static bool operator !=(DocumentIndex left, DocumentIndex right) => left.RowId != right.RowId;

        public static explicit operator DocumentIndex(int value) => new DocumentIndex(value);

        //Can't use implicit operator here, as for some reason this has a backwards effect of allowing other indices to be passed to our tables, due to the presence of a general purpose int indexer
        public static explicit operator int(DocumentIndex value) => value.RowId;

        public override bool Equals(object other)
        {
            if (other is not DocumentIndex v)
                return false;

            return Equals(v);
        }

        public bool Equals(DocumentIndex other) => RowId == other.RowId;

        public override int GetHashCode() => RowId.GetHashCode();

        public override string ToString() => RowId.ToString();
    }

    public readonly partial struct EncLogIndex : IEquatable<EncLogIndex>
    {
        public readonly int RowId;

        public bool IsNil => RowId == 0;

        public EncLogIndex(int rowId)
        {
            RowId = rowId;
        }

        public static bool operator ==(EncLogIndex left, EncLogIndex right) => left.RowId == right.RowId;

        public static bool operator !=(EncLogIndex left, EncLogIndex right) => left.RowId != right.RowId;

        public static explicit operator EncLogIndex(int value) => new EncLogIndex(value);

        //Can't use implicit operator here, as for some reason this has a backwards effect of allowing other indices to be passed to our tables, due to the presence of a general purpose int indexer
        public static explicit operator int(EncLogIndex value) => value.RowId;

        public override bool Equals(object other)
        {
            if (other is not EncLogIndex v)
                return false;

            return Equals(v);
        }

        public bool Equals(EncLogIndex other) => RowId == other.RowId;

        public override int GetHashCode() => RowId.GetHashCode();

        public override string ToString() => RowId.ToString();
    }

    public readonly partial struct EncMapIndex : IEquatable<EncMapIndex>
    {
        public readonly int RowId;

        public bool IsNil => RowId == 0;

        public EncMapIndex(int rowId)
        {
            RowId = rowId;
        }

        public static bool operator ==(EncMapIndex left, EncMapIndex right) => left.RowId == right.RowId;

        public static bool operator !=(EncMapIndex left, EncMapIndex right) => left.RowId != right.RowId;

        public static explicit operator EncMapIndex(int value) => new EncMapIndex(value);

        //Can't use implicit operator here, as for some reason this has a backwards effect of allowing other indices to be passed to our tables, due to the presence of a general purpose int indexer
        public static explicit operator int(EncMapIndex value) => value.RowId;

        public override bool Equals(object other)
        {
            if (other is not EncMapIndex v)
                return false;

            return Equals(v);
        }

        public bool Equals(EncMapIndex other) => RowId == other.RowId;

        public override int GetHashCode() => RowId.GetHashCode();

        public override string ToString() => RowId.ToString();
    }

    public readonly partial struct EventMapIndex : IEquatable<EventMapIndex>
    {
        public readonly int RowId;

        public bool IsNil => RowId == 0;

        public EventMapIndex(int rowId)
        {
            RowId = rowId;
        }

        public static bool operator ==(EventMapIndex left, EventMapIndex right) => left.RowId == right.RowId;

        public static bool operator !=(EventMapIndex left, EventMapIndex right) => left.RowId != right.RowId;

        public static explicit operator EventMapIndex(int value) => new EventMapIndex(value);

        //Can't use implicit operator here, as for some reason this has a backwards effect of allowing other indices to be passed to our tables, due to the presence of a general purpose int indexer
        public static explicit operator int(EventMapIndex value) => value.RowId;

        public override bool Equals(object other)
        {
            if (other is not EventMapIndex v)
                return false;

            return Equals(v);
        }

        public bool Equals(EventMapIndex other) => RowId == other.RowId;

        public override int GetHashCode() => RowId.GetHashCode();

        public override string ToString() => RowId.ToString();
    }

    public readonly partial struct EventPtrIndex : IEquatable<EventPtrIndex>
    {
        public readonly int RowId;

        public bool IsNil => RowId == 0;

        public EventPtrIndex(int rowId)
        {
            RowId = rowId;
        }

        public static bool operator ==(EventPtrIndex left, EventPtrIndex right) => left.RowId == right.RowId;

        public static bool operator !=(EventPtrIndex left, EventPtrIndex right) => left.RowId != right.RowId;

        public static explicit operator EventPtrIndex(int value) => new EventPtrIndex(value);

        //Can't use implicit operator here, as for some reason this has a backwards effect of allowing other indices to be passed to our tables, due to the presence of a general purpose int indexer
        public static explicit operator int(EventPtrIndex value) => value.RowId;

        public override bool Equals(object other)
        {
            if (other is not EventPtrIndex v)
                return false;

            return Equals(v);
        }

        public bool Equals(EventPtrIndex other) => RowId == other.RowId;

        public override int GetHashCode() => RowId.GetHashCode();

        public override string ToString() => RowId.ToString();
    }

    public readonly partial struct EventIndex : IEquatable<EventIndex>
    {
        public readonly int RowId;

        public bool IsNil => RowId == 0;

        public EventIndex(int rowId)
        {
            RowId = rowId;
        }

        public static bool operator ==(EventIndex left, EventIndex right) => left.RowId == right.RowId;

        public static bool operator !=(EventIndex left, EventIndex right) => left.RowId != right.RowId;

        public static explicit operator EventIndex(int value) => new EventIndex(value);

        //Can't use implicit operator here, as for some reason this has a backwards effect of allowing other indices to be passed to our tables, due to the presence of a general purpose int indexer
        public static explicit operator int(EventIndex value) => value.RowId;

        public override bool Equals(object other)
        {
            if (other is not EventIndex v)
                return false;

            return Equals(v);
        }

        public bool Equals(EventIndex other) => RowId == other.RowId;

        public override int GetHashCode() => RowId.GetHashCode();

        public override string ToString() => RowId.ToString();
    }

    public readonly partial struct ExportedTypeIndex : IEquatable<ExportedTypeIndex>
    {
        public readonly int RowId;

        public bool IsNil => RowId == 0;

        public ExportedTypeIndex(int rowId)
        {
            RowId = rowId;
        }

        public static bool operator ==(ExportedTypeIndex left, ExportedTypeIndex right) => left.RowId == right.RowId;

        public static bool operator !=(ExportedTypeIndex left, ExportedTypeIndex right) => left.RowId != right.RowId;

        public static explicit operator ExportedTypeIndex(int value) => new ExportedTypeIndex(value);

        //Can't use implicit operator here, as for some reason this has a backwards effect of allowing other indices to be passed to our tables, due to the presence of a general purpose int indexer
        public static explicit operator int(ExportedTypeIndex value) => value.RowId;

        public override bool Equals(object other)
        {
            if (other is not ExportedTypeIndex v)
                return false;

            return Equals(v);
        }

        public bool Equals(ExportedTypeIndex other) => RowId == other.RowId;

        public override int GetHashCode() => RowId.GetHashCode();

        public override string ToString() => RowId.ToString();
    }

    public readonly partial struct FieldLayoutIndex : IEquatable<FieldLayoutIndex>
    {
        public readonly int RowId;

        public bool IsNil => RowId == 0;

        public FieldLayoutIndex(int rowId)
        {
            RowId = rowId;
        }

        public static bool operator ==(FieldLayoutIndex left, FieldLayoutIndex right) => left.RowId == right.RowId;

        public static bool operator !=(FieldLayoutIndex left, FieldLayoutIndex right) => left.RowId != right.RowId;

        public static explicit operator FieldLayoutIndex(int value) => new FieldLayoutIndex(value);

        //Can't use implicit operator here, as for some reason this has a backwards effect of allowing other indices to be passed to our tables, due to the presence of a general purpose int indexer
        public static explicit operator int(FieldLayoutIndex value) => value.RowId;

        public override bool Equals(object other)
        {
            if (other is not FieldLayoutIndex v)
                return false;

            return Equals(v);
        }

        public bool Equals(FieldLayoutIndex other) => RowId == other.RowId;

        public override int GetHashCode() => RowId.GetHashCode();

        public override string ToString() => RowId.ToString();
    }

    public readonly partial struct FieldMarshalIndex : IEquatable<FieldMarshalIndex>
    {
        public readonly int RowId;

        public bool IsNil => RowId == 0;

        public FieldMarshalIndex(int rowId)
        {
            RowId = rowId;
        }

        public static bool operator ==(FieldMarshalIndex left, FieldMarshalIndex right) => left.RowId == right.RowId;

        public static bool operator !=(FieldMarshalIndex left, FieldMarshalIndex right) => left.RowId != right.RowId;

        public static explicit operator FieldMarshalIndex(int value) => new FieldMarshalIndex(value);

        //Can't use implicit operator here, as for some reason this has a backwards effect of allowing other indices to be passed to our tables, due to the presence of a general purpose int indexer
        public static explicit operator int(FieldMarshalIndex value) => value.RowId;

        public override bool Equals(object other)
        {
            if (other is not FieldMarshalIndex v)
                return false;

            return Equals(v);
        }

        public bool Equals(FieldMarshalIndex other) => RowId == other.RowId;

        public override int GetHashCode() => RowId.GetHashCode();

        public override string ToString() => RowId.ToString();
    }

    public readonly partial struct FieldPtrIndex : IEquatable<FieldPtrIndex>
    {
        public readonly int RowId;

        public bool IsNil => RowId == 0;

        public FieldPtrIndex(int rowId)
        {
            RowId = rowId;
        }

        public static bool operator ==(FieldPtrIndex left, FieldPtrIndex right) => left.RowId == right.RowId;

        public static bool operator !=(FieldPtrIndex left, FieldPtrIndex right) => left.RowId != right.RowId;

        public static explicit operator FieldPtrIndex(int value) => new FieldPtrIndex(value);

        //Can't use implicit operator here, as for some reason this has a backwards effect of allowing other indices to be passed to our tables, due to the presence of a general purpose int indexer
        public static explicit operator int(FieldPtrIndex value) => value.RowId;

        public override bool Equals(object other)
        {
            if (other is not FieldPtrIndex v)
                return false;

            return Equals(v);
        }

        public bool Equals(FieldPtrIndex other) => RowId == other.RowId;

        public override int GetHashCode() => RowId.GetHashCode();

        public override string ToString() => RowId.ToString();
    }

    public readonly partial struct FieldIndex : IEquatable<FieldIndex>
    {
        public readonly int RowId;

        public bool IsNil => RowId == 0;

        public FieldIndex(int rowId)
        {
            RowId = rowId;
        }

        public static bool operator ==(FieldIndex left, FieldIndex right) => left.RowId == right.RowId;

        public static bool operator !=(FieldIndex left, FieldIndex right) => left.RowId != right.RowId;

        public static explicit operator FieldIndex(int value) => new FieldIndex(value);

        //Can't use implicit operator here, as for some reason this has a backwards effect of allowing other indices to be passed to our tables, due to the presence of a general purpose int indexer
        public static explicit operator int(FieldIndex value) => value.RowId;

        public override bool Equals(object other)
        {
            if (other is not FieldIndex v)
                return false;

            return Equals(v);
        }

        public bool Equals(FieldIndex other) => RowId == other.RowId;

        public override int GetHashCode() => RowId.GetHashCode();

        public override string ToString() => RowId.ToString();
    }

    public readonly partial struct FieldRvaIndex : IEquatable<FieldRvaIndex>
    {
        public readonly int RowId;

        public bool IsNil => RowId == 0;

        public FieldRvaIndex(int rowId)
        {
            RowId = rowId;
        }

        public static bool operator ==(FieldRvaIndex left, FieldRvaIndex right) => left.RowId == right.RowId;

        public static bool operator !=(FieldRvaIndex left, FieldRvaIndex right) => left.RowId != right.RowId;

        public static explicit operator FieldRvaIndex(int value) => new FieldRvaIndex(value);

        //Can't use implicit operator here, as for some reason this has a backwards effect of allowing other indices to be passed to our tables, due to the presence of a general purpose int indexer
        public static explicit operator int(FieldRvaIndex value) => value.RowId;

        public override bool Equals(object other)
        {
            if (other is not FieldRvaIndex v)
                return false;

            return Equals(v);
        }

        public bool Equals(FieldRvaIndex other) => RowId == other.RowId;

        public override int GetHashCode() => RowId.GetHashCode();

        public override string ToString() => RowId.ToString();
    }

    public readonly partial struct FileIndex : IEquatable<FileIndex>
    {
        public readonly int RowId;

        public bool IsNil => RowId == 0;

        public FileIndex(int rowId)
        {
            RowId = rowId;
        }

        public static bool operator ==(FileIndex left, FileIndex right) => left.RowId == right.RowId;

        public static bool operator !=(FileIndex left, FileIndex right) => left.RowId != right.RowId;

        public static explicit operator FileIndex(int value) => new FileIndex(value);

        //Can't use implicit operator here, as for some reason this has a backwards effect of allowing other indices to be passed to our tables, due to the presence of a general purpose int indexer
        public static explicit operator int(FileIndex value) => value.RowId;

        public override bool Equals(object other)
        {
            if (other is not FileIndex v)
                return false;

            return Equals(v);
        }

        public bool Equals(FileIndex other) => RowId == other.RowId;

        public override int GetHashCode() => RowId.GetHashCode();

        public override string ToString() => RowId.ToString();
    }

    public readonly partial struct GenericParamConstraintIndex : IEquatable<GenericParamConstraintIndex>
    {
        public readonly int RowId;

        public bool IsNil => RowId == 0;

        public GenericParamConstraintIndex(int rowId)
        {
            RowId = rowId;
        }

        public static bool operator ==(GenericParamConstraintIndex left, GenericParamConstraintIndex right) => left.RowId == right.RowId;

        public static bool operator !=(GenericParamConstraintIndex left, GenericParamConstraintIndex right) => left.RowId != right.RowId;

        public static explicit operator GenericParamConstraintIndex(int value) => new GenericParamConstraintIndex(value);

        //Can't use implicit operator here, as for some reason this has a backwards effect of allowing other indices to be passed to our tables, due to the presence of a general purpose int indexer
        public static explicit operator int(GenericParamConstraintIndex value) => value.RowId;

        public override bool Equals(object other)
        {
            if (other is not GenericParamConstraintIndex v)
                return false;

            return Equals(v);
        }

        public bool Equals(GenericParamConstraintIndex other) => RowId == other.RowId;

        public override int GetHashCode() => RowId.GetHashCode();

        public override string ToString() => RowId.ToString();
    }

    public readonly partial struct GenericParamIndex : IEquatable<GenericParamIndex>
    {
        public readonly int RowId;

        public bool IsNil => RowId == 0;

        public GenericParamIndex(int rowId)
        {
            RowId = rowId;
        }

        public static bool operator ==(GenericParamIndex left, GenericParamIndex right) => left.RowId == right.RowId;

        public static bool operator !=(GenericParamIndex left, GenericParamIndex right) => left.RowId != right.RowId;

        public static explicit operator GenericParamIndex(int value) => new GenericParamIndex(value);

        //Can't use implicit operator here, as for some reason this has a backwards effect of allowing other indices to be passed to our tables, due to the presence of a general purpose int indexer
        public static explicit operator int(GenericParamIndex value) => value.RowId;

        public override bool Equals(object other)
        {
            if (other is not GenericParamIndex v)
                return false;

            return Equals(v);
        }

        public bool Equals(GenericParamIndex other) => RowId == other.RowId;

        public override int GetHashCode() => RowId.GetHashCode();

        public override string ToString() => RowId.ToString();
    }

    public readonly partial struct ImplMapIndex : IEquatable<ImplMapIndex>
    {
        public readonly int RowId;

        public bool IsNil => RowId == 0;

        public ImplMapIndex(int rowId)
        {
            RowId = rowId;
        }

        public static bool operator ==(ImplMapIndex left, ImplMapIndex right) => left.RowId == right.RowId;

        public static bool operator !=(ImplMapIndex left, ImplMapIndex right) => left.RowId != right.RowId;

        public static explicit operator ImplMapIndex(int value) => new ImplMapIndex(value);

        //Can't use implicit operator here, as for some reason this has a backwards effect of allowing other indices to be passed to our tables, due to the presence of a general purpose int indexer
        public static explicit operator int(ImplMapIndex value) => value.RowId;

        public override bool Equals(object other)
        {
            if (other is not ImplMapIndex v)
                return false;

            return Equals(v);
        }

        public bool Equals(ImplMapIndex other) => RowId == other.RowId;

        public override int GetHashCode() => RowId.GetHashCode();

        public override string ToString() => RowId.ToString();
    }

    public readonly partial struct ImportScopeIndex : IEquatable<ImportScopeIndex>
    {
        public readonly int RowId;

        public bool IsNil => RowId == 0;

        public ImportScopeIndex(int rowId)
        {
            RowId = rowId;
        }

        public static bool operator ==(ImportScopeIndex left, ImportScopeIndex right) => left.RowId == right.RowId;

        public static bool operator !=(ImportScopeIndex left, ImportScopeIndex right) => left.RowId != right.RowId;

        public static explicit operator ImportScopeIndex(int value) => new ImportScopeIndex(value);

        //Can't use implicit operator here, as for some reason this has a backwards effect of allowing other indices to be passed to our tables, due to the presence of a general purpose int indexer
        public static explicit operator int(ImportScopeIndex value) => value.RowId;

        public override bool Equals(object other)
        {
            if (other is not ImportScopeIndex v)
                return false;

            return Equals(v);
        }

        public bool Equals(ImportScopeIndex other) => RowId == other.RowId;

        public override int GetHashCode() => RowId.GetHashCode();

        public override string ToString() => RowId.ToString();
    }

    public readonly partial struct InterfaceImplIndex : IEquatable<InterfaceImplIndex>
    {
        public readonly int RowId;

        public bool IsNil => RowId == 0;

        public InterfaceImplIndex(int rowId)
        {
            RowId = rowId;
        }

        public static bool operator ==(InterfaceImplIndex left, InterfaceImplIndex right) => left.RowId == right.RowId;

        public static bool operator !=(InterfaceImplIndex left, InterfaceImplIndex right) => left.RowId != right.RowId;

        public static explicit operator InterfaceImplIndex(int value) => new InterfaceImplIndex(value);

        //Can't use implicit operator here, as for some reason this has a backwards effect of allowing other indices to be passed to our tables, due to the presence of a general purpose int indexer
        public static explicit operator int(InterfaceImplIndex value) => value.RowId;

        public override bool Equals(object other)
        {
            if (other is not InterfaceImplIndex v)
                return false;

            return Equals(v);
        }

        public bool Equals(InterfaceImplIndex other) => RowId == other.RowId;

        public override int GetHashCode() => RowId.GetHashCode();

        public override string ToString() => RowId.ToString();
    }

    public readonly partial struct LocalConstantIndex : IEquatable<LocalConstantIndex>
    {
        public readonly int RowId;

        public bool IsNil => RowId == 0;

        public LocalConstantIndex(int rowId)
        {
            RowId = rowId;
        }

        public static bool operator ==(LocalConstantIndex left, LocalConstantIndex right) => left.RowId == right.RowId;

        public static bool operator !=(LocalConstantIndex left, LocalConstantIndex right) => left.RowId != right.RowId;

        public static explicit operator LocalConstantIndex(int value) => new LocalConstantIndex(value);

        //Can't use implicit operator here, as for some reason this has a backwards effect of allowing other indices to be passed to our tables, due to the presence of a general purpose int indexer
        public static explicit operator int(LocalConstantIndex value) => value.RowId;

        public override bool Equals(object other)
        {
            if (other is not LocalConstantIndex v)
                return false;

            return Equals(v);
        }

        public bool Equals(LocalConstantIndex other) => RowId == other.RowId;

        public override int GetHashCode() => RowId.GetHashCode();

        public override string ToString() => RowId.ToString();
    }

    public readonly partial struct LocalScopeIndex : IEquatable<LocalScopeIndex>
    {
        public readonly int RowId;

        public bool IsNil => RowId == 0;

        public LocalScopeIndex(int rowId)
        {
            RowId = rowId;
        }

        public static bool operator ==(LocalScopeIndex left, LocalScopeIndex right) => left.RowId == right.RowId;

        public static bool operator !=(LocalScopeIndex left, LocalScopeIndex right) => left.RowId != right.RowId;

        public static explicit operator LocalScopeIndex(int value) => new LocalScopeIndex(value);

        //Can't use implicit operator here, as for some reason this has a backwards effect of allowing other indices to be passed to our tables, due to the presence of a general purpose int indexer
        public static explicit operator int(LocalScopeIndex value) => value.RowId;

        public override bool Equals(object other)
        {
            if (other is not LocalScopeIndex v)
                return false;

            return Equals(v);
        }

        public bool Equals(LocalScopeIndex other) => RowId == other.RowId;

        public override int GetHashCode() => RowId.GetHashCode();

        public override string ToString() => RowId.ToString();
    }

    public readonly partial struct LocalVariableIndex : IEquatable<LocalVariableIndex>
    {
        public readonly int RowId;

        public bool IsNil => RowId == 0;

        public LocalVariableIndex(int rowId)
        {
            RowId = rowId;
        }

        public static bool operator ==(LocalVariableIndex left, LocalVariableIndex right) => left.RowId == right.RowId;

        public static bool operator !=(LocalVariableIndex left, LocalVariableIndex right) => left.RowId != right.RowId;

        public static explicit operator LocalVariableIndex(int value) => new LocalVariableIndex(value);

        //Can't use implicit operator here, as for some reason this has a backwards effect of allowing other indices to be passed to our tables, due to the presence of a general purpose int indexer
        public static explicit operator int(LocalVariableIndex value) => value.RowId;

        public override bool Equals(object other)
        {
            if (other is not LocalVariableIndex v)
                return false;

            return Equals(v);
        }

        public bool Equals(LocalVariableIndex other) => RowId == other.RowId;

        public override int GetHashCode() => RowId.GetHashCode();

        public override string ToString() => RowId.ToString();
    }

    public readonly partial struct ManifestResourceIndex : IEquatable<ManifestResourceIndex>
    {
        public readonly int RowId;

        public bool IsNil => RowId == 0;

        public ManifestResourceIndex(int rowId)
        {
            RowId = rowId;
        }

        public static bool operator ==(ManifestResourceIndex left, ManifestResourceIndex right) => left.RowId == right.RowId;

        public static bool operator !=(ManifestResourceIndex left, ManifestResourceIndex right) => left.RowId != right.RowId;

        public static explicit operator ManifestResourceIndex(int value) => new ManifestResourceIndex(value);

        //Can't use implicit operator here, as for some reason this has a backwards effect of allowing other indices to be passed to our tables, due to the presence of a general purpose int indexer
        public static explicit operator int(ManifestResourceIndex value) => value.RowId;

        public override bool Equals(object other)
        {
            if (other is not ManifestResourceIndex v)
                return false;

            return Equals(v);
        }

        public bool Equals(ManifestResourceIndex other) => RowId == other.RowId;

        public override int GetHashCode() => RowId.GetHashCode();

        public override string ToString() => RowId.ToString();
    }

    public readonly partial struct MemberRefIndex : IEquatable<MemberRefIndex>
    {
        public readonly int RowId;

        public bool IsNil => RowId == 0;

        public MemberRefIndex(int rowId)
        {
            RowId = rowId;
        }

        public static bool operator ==(MemberRefIndex left, MemberRefIndex right) => left.RowId == right.RowId;

        public static bool operator !=(MemberRefIndex left, MemberRefIndex right) => left.RowId != right.RowId;

        public static explicit operator MemberRefIndex(int value) => new MemberRefIndex(value);

        //Can't use implicit operator here, as for some reason this has a backwards effect of allowing other indices to be passed to our tables, due to the presence of a general purpose int indexer
        public static explicit operator int(MemberRefIndex value) => value.RowId;

        public override bool Equals(object other)
        {
            if (other is not MemberRefIndex v)
                return false;

            return Equals(v);
        }

        public bool Equals(MemberRefIndex other) => RowId == other.RowId;

        public override int GetHashCode() => RowId.GetHashCode();

        public override string ToString() => RowId.ToString();
    }

    public readonly partial struct MethodDebugInformationIndex : IEquatable<MethodDebugInformationIndex>
    {
        public readonly int RowId;

        public bool IsNil => RowId == 0;

        public MethodDebugInformationIndex(int rowId)
        {
            RowId = rowId;
        }

        public static bool operator ==(MethodDebugInformationIndex left, MethodDebugInformationIndex right) => left.RowId == right.RowId;

        public static bool operator !=(MethodDebugInformationIndex left, MethodDebugInformationIndex right) => left.RowId != right.RowId;

        public static explicit operator MethodDebugInformationIndex(int value) => new MethodDebugInformationIndex(value);

        //Can't use implicit operator here, as for some reason this has a backwards effect of allowing other indices to be passed to our tables, due to the presence of a general purpose int indexer
        public static explicit operator int(MethodDebugInformationIndex value) => value.RowId;

        public override bool Equals(object other)
        {
            if (other is not MethodDebugInformationIndex v)
                return false;

            return Equals(v);
        }

        public bool Equals(MethodDebugInformationIndex other) => RowId == other.RowId;

        public override int GetHashCode() => RowId.GetHashCode();

        public override string ToString() => RowId.ToString();
    }

    public readonly partial struct MethodDefIndex : IEquatable<MethodDefIndex>
    {
        public readonly int RowId;

        public bool IsNil => RowId == 0;

        public MethodDefIndex(int rowId)
        {
            RowId = rowId;
        }

        public static bool operator ==(MethodDefIndex left, MethodDefIndex right) => left.RowId == right.RowId;

        public static bool operator !=(MethodDefIndex left, MethodDefIndex right) => left.RowId != right.RowId;

        public static explicit operator MethodDefIndex(int value) => new MethodDefIndex(value);

        //Can't use implicit operator here, as for some reason this has a backwards effect of allowing other indices to be passed to our tables, due to the presence of a general purpose int indexer
        public static explicit operator int(MethodDefIndex value) => value.RowId;

        public override bool Equals(object other)
        {
            if (other is not MethodDefIndex v)
                return false;

            return Equals(v);
        }

        public bool Equals(MethodDefIndex other) => RowId == other.RowId;

        public override int GetHashCode() => RowId.GetHashCode();

        public override string ToString() => RowId.ToString();
    }

    public readonly partial struct MethodImplIndex : IEquatable<MethodImplIndex>
    {
        public readonly int RowId;

        public bool IsNil => RowId == 0;

        public MethodImplIndex(int rowId)
        {
            RowId = rowId;
        }

        public static bool operator ==(MethodImplIndex left, MethodImplIndex right) => left.RowId == right.RowId;

        public static bool operator !=(MethodImplIndex left, MethodImplIndex right) => left.RowId != right.RowId;

        public static explicit operator MethodImplIndex(int value) => new MethodImplIndex(value);

        //Can't use implicit operator here, as for some reason this has a backwards effect of allowing other indices to be passed to our tables, due to the presence of a general purpose int indexer
        public static explicit operator int(MethodImplIndex value) => value.RowId;

        public override bool Equals(object other)
        {
            if (other is not MethodImplIndex v)
                return false;

            return Equals(v);
        }

        public bool Equals(MethodImplIndex other) => RowId == other.RowId;

        public override int GetHashCode() => RowId.GetHashCode();

        public override string ToString() => RowId.ToString();
    }

    public readonly partial struct MethodPtrIndex : IEquatable<MethodPtrIndex>
    {
        public readonly int RowId;

        public bool IsNil => RowId == 0;

        public MethodPtrIndex(int rowId)
        {
            RowId = rowId;
        }

        public static bool operator ==(MethodPtrIndex left, MethodPtrIndex right) => left.RowId == right.RowId;

        public static bool operator !=(MethodPtrIndex left, MethodPtrIndex right) => left.RowId != right.RowId;

        public static explicit operator MethodPtrIndex(int value) => new MethodPtrIndex(value);

        //Can't use implicit operator here, as for some reason this has a backwards effect of allowing other indices to be passed to our tables, due to the presence of a general purpose int indexer
        public static explicit operator int(MethodPtrIndex value) => value.RowId;

        public override bool Equals(object other)
        {
            if (other is not MethodPtrIndex v)
                return false;

            return Equals(v);
        }

        public bool Equals(MethodPtrIndex other) => RowId == other.RowId;

        public override int GetHashCode() => RowId.GetHashCode();

        public override string ToString() => RowId.ToString();
    }

    public readonly partial struct MethodSemanticsIndex : IEquatable<MethodSemanticsIndex>
    {
        public readonly int RowId;

        public bool IsNil => RowId == 0;

        public MethodSemanticsIndex(int rowId)
        {
            RowId = rowId;
        }

        public static bool operator ==(MethodSemanticsIndex left, MethodSemanticsIndex right) => left.RowId == right.RowId;

        public static bool operator !=(MethodSemanticsIndex left, MethodSemanticsIndex right) => left.RowId != right.RowId;

        public static explicit operator MethodSemanticsIndex(int value) => new MethodSemanticsIndex(value);

        //Can't use implicit operator here, as for some reason this has a backwards effect of allowing other indices to be passed to our tables, due to the presence of a general purpose int indexer
        public static explicit operator int(MethodSemanticsIndex value) => value.RowId;

        public override bool Equals(object other)
        {
            if (other is not MethodSemanticsIndex v)
                return false;

            return Equals(v);
        }

        public bool Equals(MethodSemanticsIndex other) => RowId == other.RowId;

        public override int GetHashCode() => RowId.GetHashCode();

        public override string ToString() => RowId.ToString();
    }

    public readonly partial struct MethodSpecIndex : IEquatable<MethodSpecIndex>
    {
        public readonly int RowId;

        public bool IsNil => RowId == 0;

        public MethodSpecIndex(int rowId)
        {
            RowId = rowId;
        }

        public static bool operator ==(MethodSpecIndex left, MethodSpecIndex right) => left.RowId == right.RowId;

        public static bool operator !=(MethodSpecIndex left, MethodSpecIndex right) => left.RowId != right.RowId;

        public static explicit operator MethodSpecIndex(int value) => new MethodSpecIndex(value);

        //Can't use implicit operator here, as for some reason this has a backwards effect of allowing other indices to be passed to our tables, due to the presence of a general purpose int indexer
        public static explicit operator int(MethodSpecIndex value) => value.RowId;

        public override bool Equals(object other)
        {
            if (other is not MethodSpecIndex v)
                return false;

            return Equals(v);
        }

        public bool Equals(MethodSpecIndex other) => RowId == other.RowId;

        public override int GetHashCode() => RowId.GetHashCode();

        public override string ToString() => RowId.ToString();
    }

    public readonly partial struct ModuleRefIndex : IEquatable<ModuleRefIndex>
    {
        public readonly int RowId;

        public bool IsNil => RowId == 0;

        public ModuleRefIndex(int rowId)
        {
            RowId = rowId;
        }

        public static bool operator ==(ModuleRefIndex left, ModuleRefIndex right) => left.RowId == right.RowId;

        public static bool operator !=(ModuleRefIndex left, ModuleRefIndex right) => left.RowId != right.RowId;

        public static explicit operator ModuleRefIndex(int value) => new ModuleRefIndex(value);

        //Can't use implicit operator here, as for some reason this has a backwards effect of allowing other indices to be passed to our tables, due to the presence of a general purpose int indexer
        public static explicit operator int(ModuleRefIndex value) => value.RowId;

        public override bool Equals(object other)
        {
            if (other is not ModuleRefIndex v)
                return false;

            return Equals(v);
        }

        public bool Equals(ModuleRefIndex other) => RowId == other.RowId;

        public override int GetHashCode() => RowId.GetHashCode();

        public override string ToString() => RowId.ToString();
    }

    public readonly partial struct ModuleIndex : IEquatable<ModuleIndex>
    {
        public readonly int RowId;

        public bool IsNil => RowId == 0;

        public ModuleIndex(int rowId)
        {
            RowId = rowId;
        }

        public static bool operator ==(ModuleIndex left, ModuleIndex right) => left.RowId == right.RowId;

        public static bool operator !=(ModuleIndex left, ModuleIndex right) => left.RowId != right.RowId;

        public static explicit operator ModuleIndex(int value) => new ModuleIndex(value);

        //Can't use implicit operator here, as for some reason this has a backwards effect of allowing other indices to be passed to our tables, due to the presence of a general purpose int indexer
        public static explicit operator int(ModuleIndex value) => value.RowId;

        public override bool Equals(object other)
        {
            if (other is not ModuleIndex v)
                return false;

            return Equals(v);
        }

        public bool Equals(ModuleIndex other) => RowId == other.RowId;

        public override int GetHashCode() => RowId.GetHashCode();

        public override string ToString() => RowId.ToString();
    }

    public readonly partial struct NestedClassIndex : IEquatable<NestedClassIndex>
    {
        public readonly int RowId;

        public bool IsNil => RowId == 0;

        public NestedClassIndex(int rowId)
        {
            RowId = rowId;
        }

        public static bool operator ==(NestedClassIndex left, NestedClassIndex right) => left.RowId == right.RowId;

        public static bool operator !=(NestedClassIndex left, NestedClassIndex right) => left.RowId != right.RowId;

        public static explicit operator NestedClassIndex(int value) => new NestedClassIndex(value);

        //Can't use implicit operator here, as for some reason this has a backwards effect of allowing other indices to be passed to our tables, due to the presence of a general purpose int indexer
        public static explicit operator int(NestedClassIndex value) => value.RowId;

        public override bool Equals(object other)
        {
            if (other is not NestedClassIndex v)
                return false;

            return Equals(v);
        }

        public bool Equals(NestedClassIndex other) => RowId == other.RowId;

        public override int GetHashCode() => RowId.GetHashCode();

        public override string ToString() => RowId.ToString();
    }

    public readonly partial struct ParamPtrIndex : IEquatable<ParamPtrIndex>
    {
        public readonly int RowId;

        public bool IsNil => RowId == 0;

        public ParamPtrIndex(int rowId)
        {
            RowId = rowId;
        }

        public static bool operator ==(ParamPtrIndex left, ParamPtrIndex right) => left.RowId == right.RowId;

        public static bool operator !=(ParamPtrIndex left, ParamPtrIndex right) => left.RowId != right.RowId;

        public static explicit operator ParamPtrIndex(int value) => new ParamPtrIndex(value);

        //Can't use implicit operator here, as for some reason this has a backwards effect of allowing other indices to be passed to our tables, due to the presence of a general purpose int indexer
        public static explicit operator int(ParamPtrIndex value) => value.RowId;

        public override bool Equals(object other)
        {
            if (other is not ParamPtrIndex v)
                return false;

            return Equals(v);
        }

        public bool Equals(ParamPtrIndex other) => RowId == other.RowId;

        public override int GetHashCode() => RowId.GetHashCode();

        public override string ToString() => RowId.ToString();
    }

    public readonly partial struct ParamIndex : IEquatable<ParamIndex>
    {
        public readonly int RowId;

        public bool IsNil => RowId == 0;

        public ParamIndex(int rowId)
        {
            RowId = rowId;
        }

        public static bool operator ==(ParamIndex left, ParamIndex right) => left.RowId == right.RowId;

        public static bool operator !=(ParamIndex left, ParamIndex right) => left.RowId != right.RowId;

        public static explicit operator ParamIndex(int value) => new ParamIndex(value);

        //Can't use implicit operator here, as for some reason this has a backwards effect of allowing other indices to be passed to our tables, due to the presence of a general purpose int indexer
        public static explicit operator int(ParamIndex value) => value.RowId;

        public override bool Equals(object other)
        {
            if (other is not ParamIndex v)
                return false;

            return Equals(v);
        }

        public bool Equals(ParamIndex other) => RowId == other.RowId;

        public override int GetHashCode() => RowId.GetHashCode();

        public override string ToString() => RowId.ToString();
    }

    public readonly partial struct PropertyMapIndex : IEquatable<PropertyMapIndex>
    {
        public readonly int RowId;

        public bool IsNil => RowId == 0;

        public PropertyMapIndex(int rowId)
        {
            RowId = rowId;
        }

        public static bool operator ==(PropertyMapIndex left, PropertyMapIndex right) => left.RowId == right.RowId;

        public static bool operator !=(PropertyMapIndex left, PropertyMapIndex right) => left.RowId != right.RowId;

        public static explicit operator PropertyMapIndex(int value) => new PropertyMapIndex(value);

        //Can't use implicit operator here, as for some reason this has a backwards effect of allowing other indices to be passed to our tables, due to the presence of a general purpose int indexer
        public static explicit operator int(PropertyMapIndex value) => value.RowId;

        public override bool Equals(object other)
        {
            if (other is not PropertyMapIndex v)
                return false;

            return Equals(v);
        }

        public bool Equals(PropertyMapIndex other) => RowId == other.RowId;

        public override int GetHashCode() => RowId.GetHashCode();

        public override string ToString() => RowId.ToString();
    }

    public readonly partial struct PropertyPtrIndex : IEquatable<PropertyPtrIndex>
    {
        public readonly int RowId;

        public bool IsNil => RowId == 0;

        public PropertyPtrIndex(int rowId)
        {
            RowId = rowId;
        }

        public static bool operator ==(PropertyPtrIndex left, PropertyPtrIndex right) => left.RowId == right.RowId;

        public static bool operator !=(PropertyPtrIndex left, PropertyPtrIndex right) => left.RowId != right.RowId;

        public static explicit operator PropertyPtrIndex(int value) => new PropertyPtrIndex(value);

        //Can't use implicit operator here, as for some reason this has a backwards effect of allowing other indices to be passed to our tables, due to the presence of a general purpose int indexer
        public static explicit operator int(PropertyPtrIndex value) => value.RowId;

        public override bool Equals(object other)
        {
            if (other is not PropertyPtrIndex v)
                return false;

            return Equals(v);
        }

        public bool Equals(PropertyPtrIndex other) => RowId == other.RowId;

        public override int GetHashCode() => RowId.GetHashCode();

        public override string ToString() => RowId.ToString();
    }

    public readonly partial struct PropertyIndex : IEquatable<PropertyIndex>
    {
        public readonly int RowId;

        public bool IsNil => RowId == 0;

        public PropertyIndex(int rowId)
        {
            RowId = rowId;
        }

        public static bool operator ==(PropertyIndex left, PropertyIndex right) => left.RowId == right.RowId;

        public static bool operator !=(PropertyIndex left, PropertyIndex right) => left.RowId != right.RowId;

        public static explicit operator PropertyIndex(int value) => new PropertyIndex(value);

        //Can't use implicit operator here, as for some reason this has a backwards effect of allowing other indices to be passed to our tables, due to the presence of a general purpose int indexer
        public static explicit operator int(PropertyIndex value) => value.RowId;

        public override bool Equals(object other)
        {
            if (other is not PropertyIndex v)
                return false;

            return Equals(v);
        }

        public bool Equals(PropertyIndex other) => RowId == other.RowId;

        public override int GetHashCode() => RowId.GetHashCode();

        public override string ToString() => RowId.ToString();
    }

    public readonly partial struct StandAloneSigIndex : IEquatable<StandAloneSigIndex>
    {
        public readonly int RowId;

        public bool IsNil => RowId == 0;

        public StandAloneSigIndex(int rowId)
        {
            RowId = rowId;
        }

        public static bool operator ==(StandAloneSigIndex left, StandAloneSigIndex right) => left.RowId == right.RowId;

        public static bool operator !=(StandAloneSigIndex left, StandAloneSigIndex right) => left.RowId != right.RowId;

        public static explicit operator StandAloneSigIndex(int value) => new StandAloneSigIndex(value);

        //Can't use implicit operator here, as for some reason this has a backwards effect of allowing other indices to be passed to our tables, due to the presence of a general purpose int indexer
        public static explicit operator int(StandAloneSigIndex value) => value.RowId;

        public override bool Equals(object other)
        {
            if (other is not StandAloneSigIndex v)
                return false;

            return Equals(v);
        }

        public bool Equals(StandAloneSigIndex other) => RowId == other.RowId;

        public override int GetHashCode() => RowId.GetHashCode();

        public override string ToString() => RowId.ToString();
    }

    public readonly partial struct StateMachineMethodIndex : IEquatable<StateMachineMethodIndex>
    {
        public readonly int RowId;

        public bool IsNil => RowId == 0;

        public StateMachineMethodIndex(int rowId)
        {
            RowId = rowId;
        }

        public static bool operator ==(StateMachineMethodIndex left, StateMachineMethodIndex right) => left.RowId == right.RowId;

        public static bool operator !=(StateMachineMethodIndex left, StateMachineMethodIndex right) => left.RowId != right.RowId;

        public static explicit operator StateMachineMethodIndex(int value) => new StateMachineMethodIndex(value);

        //Can't use implicit operator here, as for some reason this has a backwards effect of allowing other indices to be passed to our tables, due to the presence of a general purpose int indexer
        public static explicit operator int(StateMachineMethodIndex value) => value.RowId;

        public override bool Equals(object other)
        {
            if (other is not StateMachineMethodIndex v)
                return false;

            return Equals(v);
        }

        public bool Equals(StateMachineMethodIndex other) => RowId == other.RowId;

        public override int GetHashCode() => RowId.GetHashCode();

        public override string ToString() => RowId.ToString();
    }

    public readonly partial struct TypeDefIndex : IEquatable<TypeDefIndex>
    {
        public readonly int RowId;

        public bool IsNil => RowId == 0;

        public TypeDefIndex(int rowId)
        {
            RowId = rowId;
        }

        public static bool operator ==(TypeDefIndex left, TypeDefIndex right) => left.RowId == right.RowId;

        public static bool operator !=(TypeDefIndex left, TypeDefIndex right) => left.RowId != right.RowId;

        public static explicit operator TypeDefIndex(int value) => new TypeDefIndex(value);

        //Can't use implicit operator here, as for some reason this has a backwards effect of allowing other indices to be passed to our tables, due to the presence of a general purpose int indexer
        public static explicit operator int(TypeDefIndex value) => value.RowId;

        public override bool Equals(object other)
        {
            if (other is not TypeDefIndex v)
                return false;

            return Equals(v);
        }

        public bool Equals(TypeDefIndex other) => RowId == other.RowId;

        public override int GetHashCode() => RowId.GetHashCode();

        public override string ToString() => RowId.ToString();
    }

    public readonly partial struct TypeRefIndex : IEquatable<TypeRefIndex>
    {
        public readonly int RowId;

        public bool IsNil => RowId == 0;

        public TypeRefIndex(int rowId)
        {
            RowId = rowId;
        }

        public static bool operator ==(TypeRefIndex left, TypeRefIndex right) => left.RowId == right.RowId;

        public static bool operator !=(TypeRefIndex left, TypeRefIndex right) => left.RowId != right.RowId;

        public static explicit operator TypeRefIndex(int value) => new TypeRefIndex(value);

        //Can't use implicit operator here, as for some reason this has a backwards effect of allowing other indices to be passed to our tables, due to the presence of a general purpose int indexer
        public static explicit operator int(TypeRefIndex value) => value.RowId;

        public override bool Equals(object other)
        {
            if (other is not TypeRefIndex v)
                return false;

            return Equals(v);
        }

        public bool Equals(TypeRefIndex other) => RowId == other.RowId;

        public override int GetHashCode() => RowId.GetHashCode();

        public override string ToString() => RowId.ToString();
    }

    public readonly partial struct TypeSpecIndex : IEquatable<TypeSpecIndex>
    {
        public readonly int RowId;

        public bool IsNil => RowId == 0;

        public TypeSpecIndex(int rowId)
        {
            RowId = rowId;
        }

        public static bool operator ==(TypeSpecIndex left, TypeSpecIndex right) => left.RowId == right.RowId;

        public static bool operator !=(TypeSpecIndex left, TypeSpecIndex right) => left.RowId != right.RowId;

        public static explicit operator TypeSpecIndex(int value) => new TypeSpecIndex(value);

        //Can't use implicit operator here, as for some reason this has a backwards effect of allowing other indices to be passed to our tables, due to the presence of a general purpose int indexer
        public static explicit operator int(TypeSpecIndex value) => value.RowId;

        public override bool Equals(object other)
        {
            if (other is not TypeSpecIndex v)
                return false;

            return Equals(v);
        }

        public bool Equals(TypeSpecIndex other) => RowId == other.RowId;

        public override int GetHashCode() => RowId.GetHashCode();

        public override string ToString() => RowId.ToString();
    }
}